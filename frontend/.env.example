# This is the URL of your live, deployed backend API
# (In development, you don't need to create a .env file
# because the code will fall back to localhost:5000)
VITE_API_URL=http://localhost:5000/api